# @package _global_

mlflow_artifact_path: "epi_coverage"

splits:
  - train
  - test_preliminary
  - test_final

# Key under config.mlflow_uris that points to the input tiles to annotate.
input_tiles_uri_key: "tiling"

epi_masks_path: ${project_path}/epi_segmentation/

# Name of the subfolder inside each split that holds the .tiff masks.
epi_masks_folder: "masks_mpp_1.557827"

metadata:
  run_name: "🧫 Epi Coverage"
  description: "Compute epithelium coverage per tile from existing epi-segmentation masks"
