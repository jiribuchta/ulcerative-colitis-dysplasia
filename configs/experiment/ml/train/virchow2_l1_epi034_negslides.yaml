# @package _global_

defaults:
  - /experiment/ml/train/virchow2
  - /dataset/embeddings/level1_extent224@dataset
  - _self_

dataset:
  min_thresholds:
    epithelium: 0.34

# Placeholder: negative-slides vs all-slides tile selection (not implemented yet).
negative_slides: true

metadata:
  run_name: "🏋️‍♂️ Training - ${label_mode} - virchow2 - level1 - epi 0.34 - negative slides - ${val_fold} fold"
